{
  "name": "arxiv-pdf-renamer",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check popup.js && node --check background.js"
  }
}
