{
  "name": "arxiv-pdf-renamer",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check popup.js && node --check background.js && node --check options.js"
  }
}
